{
  "name": "root",
  "private": true,
  "scripts": {
    "lerna:version": "lerna version ${LERNA_ARGS:=--yes}"
  },
  "devDependencies": {
    "husky": "^3.0.5",
    "lerna": "^3.16.4"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lerna run --concurrency 1 --stream lint-staged",
      "pre-push": "lerna run --concurrency 1 test"
    }
  }
}
